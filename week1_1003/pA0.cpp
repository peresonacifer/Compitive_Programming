#include <bits/stdc++.h>

#define F first
#define S second
#define ALL(x) begin(x), end(x)

using namespace std;
using pii = pair<int, int>;
using ll = long long;

void sol() {
    int n, k;
    cin >> n >> k;
    cout << n - k;
}

signed main() {
    ios::sync_with_stdio(0), cin.tie(0);
    int tc = 1;
    //cin >> tc;
    while (tc--) {
        sol();
    }
    return 0;
}