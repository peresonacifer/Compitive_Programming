#include <bits/stdc++.h>
using namespace std;

void sol() {
    int n;
    cin >> n;
    vector<string> ans;
    stack <char> stk1, skt2;
    for (int i = 0; i < n; i++) {
        char ch;
        while (cin.get(ch) && ch != '\n')
            if (ch == ']' || ch == ')') {
                if 
            }
            else stk1.push(ch);
         
    }

}

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    sol();
    return 0;
}
