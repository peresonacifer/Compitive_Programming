#include <bits/stdc++.h>
using namespace std;
int ar[200000];

int main() {
    ios::sync_with_stdio(0), cin.tie(0);
    int k, w;

    cin >> k >> w;
    
    
    for(int i = 0; i < k; i++) {
        cin >> ar[i];

    }
    cout << '\n';
    return 0;
}
