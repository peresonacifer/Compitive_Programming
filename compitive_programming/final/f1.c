#include<stdio.h>
#include<stdlib.h>

void f(int* ar) {

    
    // *a += 1;
    // printf("%d ", *a);
    // a += 1;
    // printf("%d %d ", a, *a);
}

int main() {
    int* ar = (int)malloc(4 * sizeof(int));

    // int n = 1;
    // int *a = &n;
    // printf("%d %d %d ", a, &n, *a);
    // f(a);
    // printf("%d %d", a, *a);
    return 0;
}